import React, { useEffect } from 'react';
import { Layout, Space, Divider, Carousel } from 'antd';
import {
  CopyrightTwoTone,
  GithubFilled,
  LinkedinFilled,
} from '@ant-design/icons';

import './mainPage.css';

const { Footer } = Layout;
const MainPage = (props) => {
  return <div className={'main-page-container'}></div>;
};

export default MainPage;
